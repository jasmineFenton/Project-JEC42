import { StyleSheet } from "react";
export const styles = StyleSheet.create({
  container: {
    justifyContent: "center",
    alignItems: "center",
    borderRadius: 4,
    borderWidth: 0.5,
    borderColor: "#d6d7da"
  },
  workspaceTitle: {
    fontSize: 19,
    fontWeight: "bold",
    alignItems: "center"
  }
});
