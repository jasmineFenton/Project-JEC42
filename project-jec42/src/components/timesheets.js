import React from "react";

export const Timesheets = props => (
  <div>
    <div class="centered">
      Timesheet stuff such as an excel sheet with labels from our product
      backlog including properties such as original hours, estimated hours etc.,
      goes here.. we can take the info from here each team member's sprint
      retrospective.
    </div>
  </div>
);
